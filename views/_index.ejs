<!DOCTYPE html>
<html>
  <head>
    <title>Welcome</title>
    <link rel='stylesheet' href='/css/main.css' />
    <link rel='stylesheet' href='/css/bootstrap.min.css' />

    <style>
      body {
          padding-top: 60px;
          padding-bottom: 40px;
      }
     </style>
  </head>
  <body>

    <div ng-view class="container">
    </div>

    <!--<% if (typeof user === 'undefined') { %>
          <h4>Welcome! Please <a href="/login">log in</a>.</h4>
        <% } else { %>
          <h4>Hello, <%= user.username %>.</h4>
          <h3>Your Projects</h3>
          <%if(typeof projects === 'undefined'){ %>
              <p>You have no Projects</p>
          <% }else{ %>
            <ul class="nav nav-tabs nav-stacked">
              <%for(var i = 0; i < projects.length; i++){ %>
                <li><a href="/project/<%=projects[i].projectId %>"><%=projects[i].projectName %></a></li>
            <% } %>
            </ul> 
           <% } %>
          <br/><a class="btn" onclick="createProject()">Create New Project</a>
      <%}%> -->

  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.8.0/jquery.min.js"></script>
        <script>window.jQuery || document.write('<script src="../js/vendor/jquery-1.8.0.min.js"><\/script>')</script>
  <script src="/js/vendor/bootstrap.min.js"></script>
  <script src="/js/vendor/bootbox.min.js"></script>
  <script>
  function createProject(){
    bootbox.prompt("What do you want to name your new project?", function(result) {
                    console.log("Result: "+result);
                    window.location="/project/create/"+result;
                });
  }
  </script>
  </body>
  
</html>


