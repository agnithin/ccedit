database structure

users{
	_id,
	userId,
	provider,
	displayName,
	email,
	projects : [{
		projectId,
		projectName
	}]
}

projects{
	_id,
	name,
	users : {
		userId,
		permissions
	}
	files: {
		fileId,
		fileName
	}
}

files{
	_id,
	name,
	contents
}

===========================
users{
	_id,
	displayName,
	email,
	provider,
	providerId,
	providerUsername,
	projects : [{
		projectId,
		name,
		description
	}]
}

projects{
	_id,
	name,
	description,
	author,
	createdOn,
	users : {
		userId,
		displayName,
		permissions
	}
	files: {
		fileId,
		name
	}
}

files{
	_id,
	name,
	contents,
	createdBy,
	createdOn,
	lastModifiedBy,
	lastModifiedOn
}

